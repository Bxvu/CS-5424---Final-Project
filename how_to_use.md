Step 1: Install the new dependencies:

Step 2: Run both programs in separate terminals:

python guesture_servo.py
python servo_control.py

Step 3: Control the arm:
Show 1-5 fingers to the camera to select which servo you want to control
Use your EEG headset's attention levels to control the angle of the selected servo
High attention (≥55) → servo moves up
Low attention (≤30) → servo moves down